# Step 1: Initialize git
git init

# Step 2: Add remote GitHub repository (replace with your URL if different)
git remote add origin https://github.com/logitechsabarni/ai-chatbot-frontend.git

# Step 3: Add all files
git add .

# Step 4: Commit changes
git commit -m "Initial commit with Vite + Tailwind + React setup"

# Step 5: Push to GitHub
git push -u origin main
